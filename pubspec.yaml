name: raya_explore_feature
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^7.0.0
  battery_plus: ^7.0.0
  mocktail: ^1.0.4
  stacked: ^3.5.0
  environment_sensors:
    path: packages/environment_sensors
  proximity_sensor: ^1.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
